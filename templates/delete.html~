{% extends "base.html" %}

{% block contents %}
<div class="container">
  <div>
    <h1><span class="fas fa-camera-retro"></span> CAMRA </h1>
  </div>
</div>

<form method="post" action="/deletesongscommit">
  {% for song in songs %}
  <input type="checkbox" name="s_id" value="{{song.s_id}}">
  <div class="container"> 
    <h3>{{song.name}} by {{song.artist}}</h3><br> 
    <audio controls="controls" id="audio_player"> 
      <source src="{{song.url}}" type="audio/mpeg"/> 
      Your browser does not support the audio element. 
    </audio> 
  </div>
  <br>
  {% endfor %}
  <input type="hidden" name="p_id" value="{{p_id}}">
  <button type="submit" class="btn btn-primary">Submit Changes</buttn>
</form>
{% endblock %}
